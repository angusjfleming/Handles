//const PersistentCollection = require("djs-collection-persistent");
//const logs = new PersistentCollection({name: "logs"});

module.exports = (bot, msg) => {
    //logs.set(msg.id, {"content":msg.content})
}